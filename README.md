# Compiler_of_Automate
A compiler for automates with specified formart

Use gcc -o main main.c to build the program

Some of the documents contains french, but it's easy to translate them into english
